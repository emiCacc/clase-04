import { NotaConColorPipe } from './nota-con-color.pipe';

describe('NotaConColorPipe', () => {
  it('create an instance', () => {
    const pipe = new NotaConColorPipe();
    expect(pipe).toBeTruthy();
  });
});
